using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    private void FixedUpdate()
    {
        // Start a new tick
        // This function will get called every Time.fixedDeltaTime seconds
        // Take a snapshot of the current game world state.




        // Package the world state and broadcast it.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
